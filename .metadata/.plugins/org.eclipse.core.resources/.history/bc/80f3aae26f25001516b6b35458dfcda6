package main;


import java.util.Arrays;


public class Solution {
	public static int canCompleteCircuit(int[] gas, int[] cost) {
		
		boolean found = true;
		
		int index[] = new int[5];
		
		for(int i = 0; i < gas.length; i++) {	//try every possible solution		
			int remaining = 0;
			found = true;
			for(int j = 0; j < gas.length; j++ ) {	//simulated running
				remaining += gas[(i+j) % gas.length];
				remaining -= cost[(i+j) % gas.length];
				if(remaining < 0) {
					found = false;
					break;
				}
			}
			if(found) {
				return i+1;
			}
		}
		
		return -1;
        
    }
	
	public static void main(String[] args) {
		int gas[] = {3,2,1};
		int cost[] = {3,2,1};
		System.out.println(canCompleteCircuit(gas, cost));
	}
}
