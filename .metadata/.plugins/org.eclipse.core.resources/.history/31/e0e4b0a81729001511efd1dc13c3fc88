package main;

public class Solution {
	public static String convert(String s, int numRows) {
		
		if(numRows == 1)
			return s;
		
        char board[][] = new char[numRows][s.length()];
        int pos = 0;
        for(int i = 0 ; i < s.length() && pos < s.length(); i ++) {
        	for(int j = 0 ; j < numRows && pos < s.length(); j++ ) {
        		
        		if(i % (numRows-1) == 0) {
        			board[j][i] = s.charAt(pos++);
        		}
        		
        		else if(j == numRows-1-(i%(numRows-1)))
        			board[j][i] = s.charAt(pos++);
        	}
        }
        
        
        String res = "";
        for(int i = 0 ; i < numRows; i++ ) {
        	for(int j = 0 ; j < s.length(); j++) {
        		res = res + board[i][j];
        		if(board[i][j] == '\0')
        			continue;
        		
        	}
        }
        return res;
    }
	
	public static void main(String[] args) {
		System.out.println(convert("viqzcdjkkwsffkjoquthpxfgrfrjetfbdvdfbmqnlisqvbglvaumxbsqgmznffojcrqdggqrrijmlqzgstv", 50));
	}
}
