package main;

public class Solution {
	public String multiply(String num1, String num2) {
        String res = "";
        int up = 0;
        
        for(int i = 0 ; i < num2.length(); i++) {
        	for(int j = num1.length()-1; j >= 0; j++) {
        		int temp = (num1.charAt(j)-'0') * (num2.charAt(i)-'0') + up;
        		res = res + (char)(temp%10+ + '0');
        		up = temp / 10;
        		
        		
        		
        		
        		
        		
        		
        	}
        }
        	
		
		
		
    }
}
