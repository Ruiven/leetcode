package main;

import java.util.List;

public class Solution {
	public List<List<String>> solveNQueens(int n) {
		return null;
    }
	
	public boolean contradict(int i) {
		return true;
	}
	
	public void place(int row, int column) {
		return;
	}
	
	public void unplace(int row, int column) {
		return;
	}
	
	public void backtrack(int row, int n, List<Integer> part, List<List<Integer>> solutions) {
		if(row == n) {
			solutions.add(part);
			return;
		}
		
		for(int i = 0 ; i < n; i++) {
			if(contradict(i))
				continue;
			place(row, i);
			backtrack(row+1, n, part, solutions);
			unplace(row, i);
		}
		
	}
}
