package main;

public class Solution {
	
	public int romanToInt(String s) {
        int out = 0;
        for(int i = 0 ; i < s.length(); i++) {
        	if(s.charAt(i) == 'M') {
        		if(i == 0 || i < s.length()-1 && s.charAt(i+1) != 'C' || i == s.length()-1)
        			out += 1000;
        		if(i < s.length()-1 && s.charAt(i+1) != 'C')
        	}
        		
        	
        }
    }
}
