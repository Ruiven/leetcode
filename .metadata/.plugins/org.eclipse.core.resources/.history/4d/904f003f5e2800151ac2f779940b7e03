package main;

class TreeNode {
	int val;
	TreeNode left;
	TreeNode right;
	TreeNode(int x) { val = x; }
}

public class Solution {
	public static void flatten(TreeNode root) {		
		if(null == root)
			return;
		
		TreeNode leftHead = root;
		while(leftHead.left != null)
			leftHead = leftHead.left;
		
		leftHead = leftHead.left = new TreeNode(root.left.val);
		
		TreeNode rightHead = root;
		while(rightHead.right != null)
			rightHead = rightHead.right;
		rightHead = rightHead.right = new TreeNode(root.right.val);
		
		dfs(root.left, leftHead);
		dfs(root.right, rightHead);
		TreeNode t = leftHead;
		while(t.right != null)
			t = t.right;
        t.right = rightHead;
        root.right = leftHead;
    }
	
	public static void dfs(TreeNode node, TreeNode head) {
		if(node == null)
			return;
		
		TreeNode t = head;
		while(t.right != null)
			t = t.right;
		
		t.right = new TreeNode(node.val);
		dfs(node.left, head);
		dfs(node.right, head);
	}
	
	public static void main(String[] args) {
		TreeNode head = new TreeNode(1);
		head.left = new TreeNode(2);
//		head.left.left = new TreeNode(3);
//		head.left.right = new TreeNode(4);
//		head.right = new TreeNode(5);
//		head.right.right = new TreeNode(6);
//		
		flatten(head);
	}
	
	
}
