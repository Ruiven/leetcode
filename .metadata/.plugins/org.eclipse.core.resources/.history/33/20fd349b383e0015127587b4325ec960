package main;

import java.util.ArrayList;
import java.util.List;


class TreeNode {
	int val;
	TreeNode left;
	TreeNode right;
	TreeNode(int x) { val = x; }
}

public class Solution {
	public List<List<Integer>> pathSum(TreeNode root, int sum) {
		List<List<Integer>> res = new ArrayList<>();
		if(root == null)
			return res;
		recursive(root, new ArrayList<Integer>(), res, sum);
		return res;
    }
	
	void recursive(TreeNode root, List<Integer> part, List<List<Integer>> res, int total, int sum) {
		if(root.left == null && root.right == null) {
			if()
		}
	}
}
